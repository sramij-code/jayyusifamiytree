/* ============================================================================
   PUBLISHED THEME — what every visitor sees.

   This file is the whole point of the admin/viewer split. Picking colours in
   admin.html only changes that browser's localStorage draft; nothing reaches
   visitors until this file is committed. Git is the real access control, not
   the password gate.

   Regenerate: admin.html -> customise -> [PUBLISH] -> replace this file -> commit.
============================================================================ */

window.FT_THEME = {
  bg:         "#f4f3ef",
  nodeFill:   "#ede8ff",
  fontColor:  "#5b21b6",
  lineColor:  "#9575c4",
  lineWidth:  1.5,
  marriage:   "#b45309"
};
